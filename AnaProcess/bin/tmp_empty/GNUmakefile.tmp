#
# This is an example GNUmakefile for my packages
#

# specific names for this package
DICT  = Package_NameCint
SHLIB = libPackage_Name.so
SOURCES = $(wildcard *.cc)
HEADERS = $(wildcard *.hh)
OBJECTS = $(SOURCES:.cc=.o)

# include options for this package
INCFLAGS = -I.                       #Include itself
INCFLAGS += -I$(ANA_PROC_DIR)/Base
INCFLAGS += -I$(ANA_PROC_DIR)/DataFormat
INCFLAGS += -I$(ANA_PROC_DIR)/Analysis

# platform-specific options
OSNAME          = $(shell uname -s)
HOST            = $(shell uname -n)
OSNAMEMODE      = $(OSNAME)

include $(ANA_PROC_DIR)/Makefile/Makefile.${OSNAME}

# set compiler options for ROOT
CXXFLAGS +=  `root-config --cflags`

# call the common GNUmakefile
include $(ANA_PROC_DIR)/Makefile/GNUmakefile.CORE



